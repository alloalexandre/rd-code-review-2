`Meteor.settings` as $meteor_settings where {
	register_diagnostic(span=$meteor_settings, message="Direct usage of Meteor.settings is forbidden. Use METEOR_SETTINGS variable instead.", severity="error")
}
