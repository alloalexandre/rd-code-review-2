`createContext` as $create_context where {
	register_diagnostic(span=$create_context, message="Prefer Zustand over React Context for global state management. Context causes unnecessary re-renders and prop drilling. Use Zustand for better performance and simpler API.", severity="warn")
}
