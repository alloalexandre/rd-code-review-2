/**
 * Below is an example of a business logic function. Functions in the 'services' directory
 * typically contain core business logic that can be reused across different parts of the application.
 *
 * Prefer named functions over default exports for better maintainability.
 */

export function myBusinessFunction(): string {
	return "Client-side business logic executed on the server";
}
