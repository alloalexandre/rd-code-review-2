import { AHMongoCollection } from "/imports/lib/allohoustonCollection";

/**
 * Example collection.
 *
 * It uses AHMongoCollection which is a wrapper around Mongo.Collection.
 *
 * You can create your own collections following this example.
 */
export const Example = new AHMongoCollection("Example");
